<shape android:shape="rectangle"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="@color/text_white"
        android:endColor="@color/text_white"
        android:angle="180">
    </gradient>

    <corners android:radius="@dimen/margin_50dp" />

    <stroke
        android:width="1px"
        android:color="@color/text_disabled" />
</shape>